# BBPhysic - 开发 TODO（Blockbench 裙摆物理预烘培/预览）

> 目标：在 Blockbench 中以插件形式提供“裙摆/布料感”的轻量物理解算，并支持：
> 1) 制作时实时预览（可选）
> 2) 将结果预烘培（Bake）为动画关键帧（核心）
>
> 备注：本项目优先面向 Blockbench Web（web.blockbench.net）可运行的插件能力；若需要更高性能再考虑 Rust→WASM。

---

## 里程碑 0：可行性与范围锁定

- [x] 明确导出目标格式（**Bedrock** / Java / glTF / 其它）与允许的动画类型（骨骼/组变换为主）
- [x] 明确裙子建模方式：
  - [x] A. 骨骼链（推荐）：每条裙摆为一条骨骼链（4–8 段）
  - [ ] B. 网格/块弹簧网络：最终仍需要映射为骨骼或可导出的动画数据
- [x] 明确碰撞需求最小集：
  - [x] 只做“裙摆点/段 vs 腿/躯干”近似碰撞（球/胶囊/盒）
  - [ ] 不做自碰撞（或仅做同链简单约束）
- [x] 明确运行环境：
  - [ ] Web 优先：不依赖 Node 原生模块/子进程
  - [x] Desktop 可选：如需桌面专属能力（见 custom/desktop.requireNativeModule 等）

验收标准：
- 文档化以上决策（写入 README 或本文件“决策记录”段）。

---

## 里程碑 1：插件骨架与入口（Action / 菜单 / 设置）

- [ ] 创建插件最小骨架：
  - [ ] 插件元信息（id/name/version/author）
  - [ ] onload / onunload 生命周期
- [ ] 注册入口命令：
  - [ ] “BBPhysic: 打开面板/设置”
  - [ ] “BBPhysic: 预览物理（开/关）”
  - [ ] “BBPhysic: 烘培到关键帧（选区/整段）”
  - 参考：custom/action.Action, custom/action.ActionOptions
- [ ] 增加最小 UI：
  - [ ] 一个对话框/面板用于参数（迭代次数、重力、阻尼、弹性、碰撞体半径等）
  - [ ] 可保存/恢复参数（后续可接 settings）

验收标准：
- 插件可在 Blockbench 中加载，命令可点击触发，弹出配置 UI。

---

## 里程碑 2：数据建模（裙摆“软体代理” + 碰撞体）

- [ ] 约定“裙摆链”来源：
  - [ ] 由用户选择骨骼/组并一键生成链配置（或按命名规则自动识别）
  - [ ] 每条链：根节点固定，末端随物理
- [ ] 设计内部数据结构：
  - [ ] 质点（位置、速度/上一帧位置、质量/权重）
  - [ ] 约束（距离约束、角度/弯曲约束、长度保持）
  - [ ] 外力（重力、风）
- [ ] 碰撞体配置：
  - [ ] 球体（中心骨骼/locator + 半径）
  - [ ] 胶囊（两点/两骨骼 + 半径）
  - [ ] 盒体（可选，先不做也行）

验收标准：
- 能从当前工程（outliner/骨骼）构建出一份可序列化的“物理配置”。

---

## 里程碑 3：解算器（JS 版本，PBD/Verlet 优先）

- [ ] 实现最小可用解算：
  - [ ] Verlet 或半隐式欧拉
  - [ ] 约束投影（PBD）：距离/长度保持
  - [ ] 阻尼（全局或每约束）
- [ ] 实现碰撞：
  - [ ] 点 vs 球（投影到表面）
  - [ ] 点 vs 胶囊（点到线段距离投影）
- [ ] 调参策略：
  - [ ] 每帧迭代次数 N（默认 6–10）
  - [ ] dt 与时间轴帧率同步（或固定 dt）

验收标准：
- 在一个简单测试模型上，裙摆不再明显穿腿，且在可接受帧率下运行。

---

## 里程碑 4：写回 Blockbench（实时预览）

- [ ] 读取当前动画/姿态：
  - [ ] 获取当前选中的 Animation / BoneAnimator（custom/animation）
  - [ ] 获取当前帧时间（custom/timeline.Timeline）
- [ ] 将解算结果写回预览：
  - [ ] 把“质点链”的姿态转换为骨骼旋转（优先只写 rotation）
  - [ ] 调用/触发预览刷新（custom/preview.animate 或相关刷新机制）

验收标准：
- 打开“预览物理”后，拖动时间轴或播放时，裙摆会随姿态实时更新。

---

## 里程碑 5：烘培（Bake）到关键帧（核心交付）

- [ ] 烘培入口：
  - [ ] 选择时间区间（起止帧）
  - [ ] 选择采样步长（每帧/每 N 帧）
- [ ] 写关键帧：
  - [ ] 为每个受控骨骼通道创建/更新 keyframe（custom/keyframe._Keyframe / KeyframeDataPoint）
  - [ ] 批量写入时使用 undo 分组（custom/undo）避免污染历史
- [ ] 稳定性：
  - [ ] 重复烘培可覆盖旧的物理 keyframes（可选：写入专用 marker 或命名约定）

验收标准：
- 点击“烘培到关键帧”，生成可回放、可导出的动画；关闭插件后动画仍保留。

---

## 里程碑 6：性能与平台策略（可选增强）

- [ ] 性能基线：
  - [ ] 记录每帧解算耗时（ms）与可承受质点数量
- [ ] 若 JS 不足：Rust → WASM：
  - [ ] Rust 实现核心解算（约束 + 碰撞）
  - [ ] 编译到 wasm，JS 插件中加载并一次调用处理一帧/多帧
  - [ ] 数据布局优化：TypedArray 传参，减少 JS↔WASM 往返
- [ ] 不推荐路线（除非强需求）：Rust 后端子进程 + IPC（分发/权限/延迟复杂）

验收标准：
- 在目标模型规模下达到可用交互帧率；若启用 WASM，明显优于纯 JS。

---

## 里程碑 7：文档与示例

- [ ] 编写使用说明：
  - [ ] 如何搭建裙摆骨骼链
  - [ ] 如何配置碰撞体
  - [ ] 如何预览与烘培
- [ ] 提供最小示例工程/截图（如允许）

---

## 决策记录（持续更新）

- 运行环境：Web / Desktop（待定）
- 物理模型：骨骼链 PBD（默认）
- 碰撞：球/胶囊（默认）
- Bake 写入策略：覆盖/追加（待定）

---

## 关键 API 参考（来自 web.blockbench.net TypeDoc）

- Action / UI 入口：custom/action
- 动画与骨骼 animator：custom/animation
- 关键帧数据：custom/keyframe
- 时间轴：custom/timeline
- 预览刷新：custom/preview
- 插件加载：generated/plugin_loader
- 桌面专属能力（可选）：custom/desktop
